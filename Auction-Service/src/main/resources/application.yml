server:
  port: 8081
spring:
  application:
    name: AuctionService
  datasource:
    url: jdbc:hsqldb:mem:auctiondb  # In-memory auction database
    driverClassName: org.hsqldb.jdbc.JDBCDriver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

